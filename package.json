{
    "name": "bca-findoc-extraction-flow",
    "version": "0.0.1",
    "description": "Flow Designer (WFA) version of the BCA Contractor Registration financial report extraction automation. Preserved as a snapshot -- installing this package currently fails on the target instance(s) with a generic 'Unable to install third party application' error that reproduces even for an empty scaffold, so this is not yet known to be a content problem. See README.md.",
    "license": "UNLICENSED",
    "imports": {
        "#now:*": "./@types/servicenow/fluent/*/index.js"
    },
    "scripts": {
        "build": "now-sdk build",
        "deploy": "now-sdk install",
        "transform": "now-sdk transform",
        "types": "now-sdk dependencies"
    },
    "devDependencies": {
        "@servicenow/sdk": "4.11.2",
        "@servicenow/glide": "27.0.5"
    }
}
